rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read access to everyone (so images show up in the app)
      allow read: if true;

      // Allow write access only to authenticated users (Staff)
      allow write: if request.auth != null;
    }
  }
}
